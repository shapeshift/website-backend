HOST=0.0.0.0
PORT=1337

APP_KEYS=key1,key2,key3,key4
API_TOKEN_SALT=
ADMIN_JWT_SECRET=
TRANSFER_TOKEN_SALT=
JWT_SECRET=
ENCRYPTION_KEY=

# Database
# For local dev, omit DATABASE_CLIENT to use SQLite
# For Railway, set to postgres and provide DATABASE_URL from the Railway Postgres plugin
# Append ?sslmode=require to DATABASE_URL if connecting via public URL
DATABASE_CLIENT=postgres
DATABASE_URL=postgresql://user:password@host:5432/dbname
